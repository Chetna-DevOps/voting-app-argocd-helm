# Configuration values used by Helm templates

replicaCount: 1

image:
  repository: <ACR_LOGIN_SERVER>/votingops/result
  tag: 419
  pullPolicy: Always

service:
  type: ClusterIP
  port: 90
  targetPort: 90

ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: result-argo-helm.<YOUR-CLUSTER-IP>.nip.io
      paths:
        - /
